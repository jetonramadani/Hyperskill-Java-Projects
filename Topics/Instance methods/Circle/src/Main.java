class Circle {

    double radius;

    // write methods here
    double getLength() {
        return Math.PI * radius * 2;
    }

    double getArea() {
        return Math.PI * radius * radius;
    }
}